# Secrets — NEVER commit (OAuth client secret + tokens)
data.json
**/data.json
*.token
# Local runtime cache (persisted Drive index snapshot)
drive-index.json
**/drive-index.json
.env
.env.*

# Build output
dist/
node_modules/
main.js

# Local dev guide — not shipped
AGENTS.md
CLAUDE.md

# Editor — personal, not shipped
.vscode/

# OS
.DS_Store
.vscode
